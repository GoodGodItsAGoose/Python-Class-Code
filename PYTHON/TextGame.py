import random
x = 0
y = 12
Item_Amt_Roll = random.randint(x, y) #roll for amount of items
class Items ():

    def ItemList ():
        def Steel_Sword ():
            ItemStats() #P{Lay though ItemStats}
            Atk1 = 8
            Atk2 = 12
            
    def ItemStats (self):
        def AttackPwr (self):
            Dmg = random.randint(Atk1, Atk2) #changes depening on weapon rolls
        def Durability (self):
            
        def Tags ():
            one = Sharpened = Dmg + random.randint(1, 3) #add boosts/drawbacks
        
if Item_Amt_Roll == 0:
    next
elif Item_Amt_Roll == 1:
    Item_Roll = random.randint(x, y) #choose from a list, roll for those items